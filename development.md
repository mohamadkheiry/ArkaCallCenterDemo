# ادامه توسعه و پشتیبانی

مبنای این شاخه نسخه پایدار پایگاه دانش متنی/فایلی است. برای شناخت محصول ابتدا
[README](README.md)، [قراردادهای معماری و فازها](CLAUDE.md) و
[معماری](docs/ARCHITECTURE.md) را مطالعه کنید.

توسعه داشبورد در `frontend` با React 19، TypeScript و Vite انجام می‌شود. نصب تکرارپذیر:

```sh
cd frontend
npm ci
npm run dev -- --host 127.0.0.1
npm run build
npm run lint
npm run preview -- --host 127.0.0.1
```

پروکسی توسعه در `vite.config.ts` به API محلی پورت ۵۰۸۰ اشاره دارد. برای تست UI بدون
پشت‌صحنه، پاسخ API را در مرورگر آزمون mock کنید؛ توکن آزمایشی یا مسیر دورزدن ورود را
داخل سورس اجرایی قرار ندهید. صفحه‌های محافظت‌شده به پاسخ معتبر `/api/me` نیاز دارند.

اجزای مشترک، رفتار راست‌چین و سناریوهای پذیرش در
[سیستم طراحی](docs/DASHBOARD_DESIGN.md) مستند شده‌اند. پس از تغییر اجزای `ui.tsx`، علاوه
بر همان صفحه، فرم ورود، راه‌اندازی، گوینده، کاربران و نسخه موبایل را بررسی کنید.
حالت موفق، loading، failure، empty، دسترسی نقش و کاهش حرکت نیز جزو قرارداد رابط‌اند.

دستورات و پیش‌نیازهای بک‌اند در [راهنمای معماری](CLAUDE.md) و روش انتشار و بازگشت در
[deployment.md](deployment.md) قرار دارند. اسرار در `.env` می‌مانند؛ در مستندات و
screenshots نمونه از داده شخصی واقعی استفاده نکنید. هر انتشار را با revision، مقصد،
بکاپ و نتیجه health در [آخرین استقرار](docs/LAST_DEPLOYMENT.md) ثبت کنید.
